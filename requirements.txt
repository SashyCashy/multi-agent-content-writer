# ── Core AI ──────────────────────────────────────────────────
openai>=1.40
langchain>=0.3
langchain-google-genai>=2.0
langgraph>=0.2

# ── Persistence (checkpointer that survives restarts) ────────
langgraph-checkpoint-sqlite>=2.0

# ── Real research / search tools ─────────────────────────────
# Optional: only needed if you set TAVILY_API_KEY.
# Without it, the tools fall back to OpenAI's hosted web_search.
tavily-python>=0.5

# ── Graph rendering (--show-graph ASCII output) ───────────────
grandalf>=0.8

# ── Config ───────────────────────────────────────────────────
python-dotenv>=1.0
pydantic>=2.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0
