# Production image only — excludes pytest, grandalf, and the optional OpenAI
# search fallback to keep the container small. Artifact Registry's free tier is
# 0.5 GB, so every dropped dependency helps.
#
# Local development still uses requirements.txt.

langchain>=0.3
langchain-google-genai>=2.0
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
tavily-python>=0.5
python-dotenv>=1.0
pydantic>=2.0

# Web server
fastapi>=0.115
uvicorn>=0.30
